{
  "name": "proconnect-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "ProConnect full-stack application",
  "main": "src/server-local.js",
  "scripts": {
    "start": "node src/server-local.js",
    "dev": "node src/server-local.js",
    "check": "npm --prefix Server run check --if-present",
    "lint": "npm --prefix Server run lint --if-present",
    "security:scan": "npm --prefix Server run security:scan --if-present",
    "build": "node -e \"console.log('No build step required for ProConnect.')\""
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^4.21.2",
    "express-rate-limit": "^8.1.0",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "mysql2": "^3.19.1"
  }
}
